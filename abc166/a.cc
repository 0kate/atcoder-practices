#include <bits/stdc++.h>

#define rep(l, r) for (int i = (l); i < (r); i++)

typedef long long ll;
 
using namespace std;

void solve() {
    string s;
    cin >> s;
    
    if (s == "ABC") {
        cout << "ARC" << endl;
    } else if (s == "ARC") {
        cout << "ABC" << endl;
    }
}

int main() {
    solve();

    return 0;
}
